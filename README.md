# LOCALHOST
>>> create application.propertis
spring.profiles.active=local
> mvn clean install
> mvn spring-boot:run

# DEVELOPMENT
>>> create application.propertis
spring.profiles.active=dev
> mvn clean install
> (mvn spring-boot:run)